<div class="container pt-3">
  <h1>Add review to <%= @flat.name %></h1>
  <%= simple_form_for [@flat, @review] do |f| %>
    <%= f.input :comment %>
    <%= f.submit "Add review" %>
  <% end %> 
</div>